require 'bundler/setup'
require 'rspec/core/rake_task'

$example_name = 'console'
import File.expand_path("../../shared/example.rake", __FILE__)

RSpec::Core::RakeTask.new(:rspec) do |t|
  t.verbose = false
end

task :rspec => :build_native
task :default => :rspec
